// import type { StrapiApp } from '@strapi/strapi/admin';


export default {
  config: {
    locales: [
      // aktifkan bahasa yang diinginkan dengan menghapus tanda komentar
      // 'id', // contoh: Bahasa Indonesia
      // 'en', // Bahasa Inggris (default)
      // 'fr', // Bahasa Prancis
    ],
  },
  // bootstrap(app: StrapiApp) {
  //   console.log(app);
  // },
};
